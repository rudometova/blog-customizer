# Проектная работа: Вёрстка проекта

## Описание проекта

Веб-приложение для настройки внешнего вида статей с помощью интерактивной панели настроек. Позволяет изменять шрифты, цвета, размеры и другие параметры отображения контента в реальном времени.

## 📋 Функциональные требования

- При нажатии на «стрелку» открывается сайдбар с настройками, при повторном нажатии или клике вне сайдбара он закрывается
- При изменении настроек в сайдбаре они не применяются сразу
- После нажатия на «Применить» стили применяются к статье
- При нажатии «Сбросить» настройки сбрасываются на начальные значения и применяются к статье
- Настройки устанавливаются через CSS-переменные
- Состояние формы сохраняется при повторном открытии сайдбара

## 🚀 Быстрый старт

### Установка зависимостей
npm install
Запуск в режиме разработки
npm start
Приложение будет доступно по адресу: http://localhost:8080

Запуск Storybook
npm run storybook
Storybook будет доступен по адресу: http://localhost:6006

Сборка для production
npm run build
🛠 Скрипты
npm start - запуск development сервера
npm run build - сборка проекта
npm run storybook - запуск Storybook
npm run lint - проверка кода линтером
npm run format - форматирование кода
npm run stylelint - проверка стилей
npm test - запуск всех проверок (стили + код)

🏗 Архитектура проекта
Структура папок
text
src/
├── components/           # React компоненты
│   ├── article/          # Компонент статьи
│   └── article-params-form/  # Форма настроек
├── constants/            # Константы и типы
├── fonts/               # Шрифты
├── images/              # Изображения
├── styles/              # Глобальные стили
├── ui/                  # UI-компоненты (кит)
│   ├── arrow-button/    # Кнопка-стрелка
│   ├── button/          # Кнопки
│   ├── select/          # Выпадающие списки
│   ├── radio-group/     # Группы переключателей
│   ├── text/            # Текстовые компоненты
│   └── separator/       # Разделители
└── index.tsx           # Точка входа

Используемые технологии
React 18 - UI библиотека
TypeScript - типизация
SCSS Modules - стилизация
Webpack - сборка
Storybook - документация компонентов

🔧 Настройки которые можно изменить
Шрифт - выбор из 5 вариантов шрифтов
Размер шрифта - 18px, 25px, 38px
Цвет текста - 9 цветов с визуальными индикаторами
Цвет фона - 9 цветов с визуальными индикаторами
Ширина контента - широкая (1394px) или узкая (948px)

🎯 Ключевые компоненты
ArticleParamsForm
Основной компонент формы настроек, содержащий:
Управление состоянием сайдбара (открыт/закрыт)
Локальное состояние формы
Обработчики изменений для всех полей
Интеграцию с CSS-переменными

Article
Компонент статьи, который отображает контент и применяет CSS-переменные для стилизации.

UI Components
Готовые компоненты из UI-кита:

ArrowButton - кнопка открытия/закрытия сайдбара

Select - выпадающие списки с поддержкой иконок

RadioGroup - группы радиокнопок

Button - кнопки действий

📝 Особенности реализации
Управление состоянием
Состояние разделено на:
Примененные настройки - хранятся в App компоненте, влияют на CSS-переменные
Локальное состояние формы - изменения не применяются сразу, только после нажатия "Применить"

CSS-переменные
Настройки применяются через CSS-переменные, объявленные в корневом элементе:
css
:root {
  --font-family: 'Open Sans';
  --font-size: '18px';
  --font-color: '#000000';
  --container-width: '1394px';
  --bg-color: '#FFFFFF';
}

Закрытие по клику вне сайдбара
Реализовано с помощью useRef и обработчика событий document.

🧪 Тестирование
Для проверки кода используйте:
npm test
Проверка TypeScript типов
npx tsc --noEmit

📚 Ссылки
Макет в Figma https://www.figma.com/file/FEeiiGLOsE7ktXbPpBxYoD/Custom-dropdown
Документация компонентов (Storybook) http://localhost:6006/